NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional — Next.js dev server auto-restarts near ~90% heap (log: "approaching the used memory threshold").
# Uncomment if the warning is noisy during local dev (chart now uses one API call, not 30).
# __NEXT_DISABLE_MEMORY_WATCHER=1
